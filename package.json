{
  "name": "memory-fragments-app",
  "version": "1.0.0",
  "description": "Memory Fragments - Personal Memory Archive App",
  "scripts": {
    "test": "firebase emulators:exec --project demo-memory-fragments --config firebase.test.json --only firestore,storage \"node --test tests/*.test.cjs\"",
    "dev": "echo 'This is a static site, no build needed'",
    "build": "node scripts/build-static.cjs",
    "preview": "echo 'Use a local server to preview'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.19.0",
    "jsdom": "26.1.0"
  }
}
